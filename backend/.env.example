DB_URL=jdbc:postgresql://localhost:5432/skautu_inventorius
DB_USER=postgres
DB_PASSWORD=change-me
# The website backend intentionally defaults to the same DB as the mobile backend.
# Keep schema changes backward-compatible with the mobile backend unless both are updated.
JWT_SECRET=change-me-to-a-long-random-secret
WEB_PORT=8081
WEB_CORS_ALLOWED_ORIGINS=http://localhost:5173
WEB_UPLOADS_DIR=uploads-web
METRICS_TOKEN=change-me-to-a-long-random-secret
RESEND_API_KEY=re_xxxxxxxxx
PASSWORD_RESET_EMAIL_FROM=Skautu inventorius <noreply@updates.example.com>
PASSWORD_RESET_PUBLIC_BASE_URL=http://localhost:5173
ACCOUNT_DELETION_PUBLIC_BASE_URL=http://localhost:5173
SETUP_BOOTSTRAP_TOKEN=change-me-bootstrap-token
